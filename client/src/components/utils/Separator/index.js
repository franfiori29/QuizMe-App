import React from 'react';
import { View, Text } from 'react-native';

const Separator = () => {
	return (
		<View
			style={{
				height: 1,
				width: '95%',
				alignSelf: 'center',
				backgroundColor: '#ccc',
			}}
		/>
	);
};

export default Separator;
